# Database (PostgreSQL)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=jobboard_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
SMS_ACTIVATION_TTL_SECONDS=600

# Email (demo)
# For real email: set EMAIL_BACKEND + SMTP credentials.
